import { StaggerButton } from "@/registry/components/buttons/stagger-button";

export default function StaggerButtonDemo() {
  return (
    <div className="flex items-center justify-center">
      <StaggerButton>Hover Me</StaggerButton>
    </div>
  );
}
